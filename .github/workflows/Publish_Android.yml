name: Build & Publish Android Apps
on: push

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: Set up Ruby v2.6
        uses: actions/setup-ruby@v1
        with:
          ruby-version: '2.6'

      - name: Setup actions/setup-node@v1 v12
        uses: actions/setup-node@v2
        with:
          node-version: '12'

      - name: Install Fastlane
        run: sudo gem install fastlane -NV

      - name: Install Bundler
        run: gem install bundler

      - name: Fastlane Beta
          run: |
            cd android
            bundle install
            cd ../

#      - name: Install Bundler and Fastlane
#        run: cd android && gem install bundler && bundle init && bundle config path vendor/bundle && bundle install && bundle add fastlane


#      - name: Configure Keystore
#        run: |
#          echo "$ANDROID_KEYSTORE_FILE" > keystore.jks.b64
#          base64 -d -i keystore.jks.b64 > app/keystore.jks
#          echo "storeFile=keystore.jks" >> keystore.properties
#          echo "keyAlias=$KEYSTORE_KEY_ALIAS" >> keystore.properties
#          echo "storePassword=$KEYSTORE_STORE_PASSWORD" >> keystore.properties
#          echo "keyPassword=$KEYSTORE_KEY_PASSWORD" >> keystore.properties
#        env:
#          ANDROID_KEYSTORE_FILE: ${{ secrets.ANDROID_KEYSTORE_FILE }}
#          KEYSTORE_KEY_ALIAS: ${{ secrets.KEYSTORE_KEY_ALIAS }}
#          KEYSTORE_KEY_PASSWORD: ${{ secrets.KEYSTORE_KEY_PASSWORD }}
#          KEYSTORE_STORE_PASSWORD: ${{ secrets.KEYSTORE_STORE_PASSWORD }}
#
#      - name: Create Google Play Config file
#        run : |
#          echo "$PLAY_CONFIG_JSON" > play_config.json.b64
#          base64 -d -i play_config.json.b64 > play_config.json
#        env:
#          PLAY_CONFIG_JSON: ${{ secrets.PLAY_CONFIG_JSON }}

      - name: Distribute app to Beta track 🚀
        run: cd android && bundle exec fastlane beta
